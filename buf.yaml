version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # #9 固定公开服务名为 HermesAgent，并复用领域消息作为响应。
    - SERVICE_SUFFIX
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
